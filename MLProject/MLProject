name: Landmine Detection

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path:
        type: string
        default: "MLProject/landmine_preprocessing.csv"
    command: "python modelling_tuning.py {data_path}"
